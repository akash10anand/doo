import React from 'react';
import { View, Text } from 'react-native';

const Settings = ({ navigation }) => {
  console.log(navigation);
  return (
    <View>
      <Text>SettingsScreen will be done later</Text>
    </View>
  );
};

export default Settings;
